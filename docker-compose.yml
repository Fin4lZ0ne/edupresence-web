services:
  app:
    build: .
    container_name: edupresence
    volumes:
      - ./:/var/www/html
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # Testing
